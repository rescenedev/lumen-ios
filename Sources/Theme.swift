import SwiftUI

extension Color {
    /// A single accent tint (set app-wide via `.tint`). Everything else uses
    /// system colors/materials so the app looks native in light and dark.
    static let lumenAccent = Color(red: 0.39, green: 0.40, blue: 0.95)
}
